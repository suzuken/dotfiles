snippet gfn
	function* ${1:name}(${2}) {
		yield ${3};
	}

snippet =>
	(${1}) => {
		${2}
	}

snippet class
	class ${1:name} {
		constructor(${2:arg}) {
			${3:// init}
		}
		${4}
	}

snippet forof
	for (let ${1:s} of ${2:sequence}) {
		${3}
	}

snippet im
	import ${1:foo} from '${2:bar}'
